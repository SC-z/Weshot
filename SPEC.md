# WeShot 产品规范

- 版本：2.0.0（build 14）
- 平台：macOS 15.2+，Apple Silicon

## 1. 产品目标

WeShot 提供低延迟、离线优先的原生 macOS 截图工作流。应用以菜单栏方式常驻，用户通过全局快捷键快速建立自定义选区，完成标注、翻译、长截图、保存、复制或钉图。

## 2. 启动与权限

- 应用以 accessory 模式运行，不显示 Dock 图标。
- 默认快捷键为 `⌃⌘A`，用户可录入包含 `⌘`、`⌃` 或 `⌥` 的组合键。
- 快捷键冲突时保留旧配置并给出明确提示。
- 未获得屏幕录制权限时显示权限说明，不循环请求。
- 截图开始前可临时隐藏 WeShot 自身浮窗，结束后恢复。

## 3. 选区

- 启动截图后冻结当前桌面并显示暗色覆盖层。
- 默认不自动选择全屏。
- 鼠标按下后直接开始自由框选，不枚举或吸附窗口边缘。
- 用户可自由拖选，并可移动或使用八个控制点缩放选区。
- 多显示器分别建立覆盖层，坐标换算支持负坐标和上下排列。

## 4. 编辑工具栏

工具顺序固定为：

1. 矩形
2. 椭圆
3. 表情
4. 箭头
5. 画笔
6. 马赛克
7. 文字
8. 翻译
9. 滚动截图
10. 撤销
11. 保存
12. 钉图
13. 取消
14. 完成

工具栏优先位于选区下方，空间不足时移动到上方，并始终限制在当前屏幕内。

## 5. 标注与取色

- 矩形、椭圆、箭头和画笔支持颜色与线宽。
- 文字标注支持直接输入；表情使用系统 Unicode 字符。
- 马赛克从原图采样并进行块状像素化。
- 所有标注使用同一预览和导出渲染路径。
- 撤销按标注创建顺序逐项移除。
- 放大镜显示光标附近像素、RGB 和 HEX；按 `C` 复制 HEX。

## 6. 翻译

- 翻译只处理当前框选区域。
- Vision 在本机提取文本，Translation 完成系统翻译。
- 译文作为可撤销的半透明图层覆盖原图，不打开独立结果窗口。
- 翻译失败时保留选区和已有标注，并显示非阻塞错误。

## 7. 隐私打码

- Vision 检测人脸和文本区域。
- 本地规则识别姓名、手机号和邮箱。
- 检测结果转换为可撤销的马赛克标注。
- 不上传屏幕图像或识别结果。

## 8. 滚动截图

- 长截图严格限制在用户选区内，不采集全屏。
- 滚轮事件继续传递给选区下方页面。
- 新帧与已有长图对齐后增量拼接，并实时更新预览。
- 重复帧不报错；无法对齐的帧被忽略并保留已有结果。
- 即使只有一帧，用户也可以点击“结束”完成。
- 完成后直接复制长图并退出截图会话，不返回首帧编辑页。
- 单次会话限制为 40 帧或约 256 MiB 原始像素数据。

## 9. 输出

- “完成”将 PNG/TIFF 写入系统剪贴板。
- “保存”通过系统保存面板原子写入 PNG。
- “钉图”默认保持截图原始尺寸，支持滚轮等比缩放、拖动和双击关闭。
- `Return` 等价于完成，`Esc` 等价于取消。

## 10. 非功能要求

- 正常截图路径不依赖网络。
- Release 构建必须在并发警告和普通警告均视为错误时通过。
- Bundle 必须通过严格代码签名校验。
- 选择、编辑和滚动交互不得留下残余进程或不可关闭窗口。
- UI 控件必须提供可读的无障碍名称。

## 11. 非目标

- 不提供独立文字识别结果窗口。
- 不提供系统分享按钮。
- 不提供演示模式。
- 不内置第三方在线翻译服务。
- 不承诺跨平台支持。

## 12. 验收标准

- 所有 Swift Testing 用例通过。
- `scripts/test_all.sh` 全部通过。
- 在已授权图形会话中，`scripts/test_system.sh` 全部通过。
- 发布压缩包回解后，版本、构建号、签名和进程自测均通过。
