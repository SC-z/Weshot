# 贡献指南

感谢参与 WeShot。

## 开发流程

1. 从 `main` 创建分支。
2. 只提交与当前问题相关的最小改动。
3. 为非平凡逻辑补充一个能够捕获回归的测试。
4. 提交前运行：

```zsh
./scripts/test_all.sh
```

涉及真实屏幕捕获、快捷键、滚动截图或系统翻译时，还应在已授权的图形会话中运行：

```zsh
./scripts/test_system.sh
```

## 提交要求

- 不提交截图、聊天记录、个人路径、账号、密钥或真实桌面内容。
- 不提交 `.build/`、`build/`、`work/`、`outputs/` 等生成目录。
- 用户可见文本应简洁，新增控件应提供无障碍名称。
- 提交信息建议使用 `feat:`、`fix:`、`refactor:`、`test:` 或 `docs:` 前缀。

## Pull Request

请说明问题、解决方案和测试结果。视觉变化应使用无隐私的合成夹具说明，不要上传真实桌面截图。
